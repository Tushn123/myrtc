log:
    log_dir: ./log
    log_name: xrtcserver
    # verbose debug info warning error
    log_level: verbose
    log_to_stderr: true
ice:
    min_port: 10025
    max_port: 10026
    # ECS / NAT: no public IPv4 on eth0 — set elastic/public IP for ICE SDP (UDP still binds 0.0.0.0)
    advertise_ip: 8.153.161.173
rtp_rtcp:
    # 单位ms
    rtcp_report_timer_interval: 100
